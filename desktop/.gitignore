node_modules/
dist/
.yarn/cache/
.yarn/install-state.gz

# The bundled engine is COPIED from the CLI by `yarn sync:cli` at build time.
# Committing it would create a second copy of the engine that drifts from the one
# published to npm - the exact thing the one-directional copy exists to prevent.
src-tauri/resources/

# Rust build output
src-tauri/target/
src-tauri/gen/
