# Supply-chain cooldown DISABLED (owner directive, fleet-wide): package managers reach TRUE
# latest with no just-published quarantine. Do NOT revert, do NOT set a non-zero value.
npmMinimalAgeGate: 0
nodeLinker: node-modules
